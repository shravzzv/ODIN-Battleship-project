/**
 * Represents the main content area for the Battleship game.
 * @returns {HTMLElement} The HTML element representing the main content area.
 */

const Main = () => {
  const element = document.createElement('main')
  element.className = 'main'

  return element
}

export default Main
